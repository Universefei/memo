# Reset 

## To look historic git commit queue

- `git log`
	- `git log --graph --oneline`

## To look master branch reference

- `cat .git/refs/heads/master`

## To reset

- `git reset --hard 9e8a761` reset to specified status
- `git reset --hard HEAD^` reset to HEAD's parent
